# Browser-side public URL ของ Hub backend (สำหรับ redirect ไป Google OAuth)
NEXT_PUBLIC_HUB_URL=http://localhost:8000

# Server-side URL ของ Hub backend (ใน Next.js API routes — ใช้ Docker network)
HUB_INTERNAL_URL=http://hub-backend:8000
