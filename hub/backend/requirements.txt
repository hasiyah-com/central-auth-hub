# ============ Web Framework ============
# bump to 0.118+ → starlette > 0.40 → fix CVE-2024-47874 + CVE-2025-54121
fastapi==0.118.0
uvicorn[standard]==0.32.0

# ============ Database ============
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.13.3

# ============ Redis ============
redis==5.2.0

# ============ Auth & Security ============
# PyJWT 2.12+ → ทดแทน python-jose (Auth0-backed) + fix PYSEC-2026-120
# ตัด ecdsa, pyasn1 ออกจาก tree → ลด CVE หลายตัว
PyJWT[crypto]==2.12.0
argon2-cffi==23.1.0
# TOTP (RFC 6238) — authenticator app เป็น fallback authentication factor
pyotp==2.9.0
# qrcode — render otpauth QR เป็น SVG inline ในหน้า Hub-served interstitial
# (pure-python, ไม่ต้อง PIL; CSP บล็อก CDN จึงต้อง render ฝั่ง server)
qrcode==8.0
# authlib 1.6.12 → fix CVE-2026-44681 (signature bypass) + 9 อื่น ๆ
authlib==1.6.12
itsdangerous==2.2.0
# Rate limiting (per-IP) — กัน brute-force / OAuth abuse
slowapi==0.1.9

# ============ Config & Validation ============
pydantic[email]==2.9.2
pydantic-settings==2.6.0
# python-dotenv 1.2.2 → fix CVE-2026-28684
python-dotenv==1.2.2
# python-multipart 0.0.27 → fix 4 CVEs (DoS / file upload bypass)
python-multipart==0.0.27

# ============ HTTP Client ============
httpx==0.27.2

# ============ Known transitive vulnerabilities (รอ upstream แก้) ============
# starlette 0.48.0 (CVE-2025-62727, PYSEC-2026-161) — fastapi 0.118 pin <0.49
# (ecdsa + pyasn1 ตัดไปแล้วหลัง migrate → PyJWT)

# ============ GeoIP (MaxMind GeoLite2 offline lookup) ============
geoip2==4.8.0

# ============ WebAuthn / Passkey (FIDO Alliance reference impl) ============
# Used in Phase 1+ — registration + authentication ceremony
webauthn==2.5.0

# ============ Seed data ============
faker==30.8.2

# ============ Email ============
aiosmtplib==3.0.2
# jinja2 3.1.6 → fix CVE-2024-56201, CVE-2024-56326, CVE-2025-27516 (template sandbox bypass)
jinja2==3.1.6

# ============ Testing (dev only — not deployed in production) ============
# pytest CVE-2025-71176 = tmp-file race ตอนรันเทสต์ ผลกระทบต่ำ + dev-only
# คงเวอร์ชั่นเดิมเพื่อ compatibility กับ pytest-asyncio
pytest==8.3.3
pytest-asyncio==0.24.0
# soft-webauthn — software authenticator สำหรับ full WebAuthn ceremony tests (Phase 6)
soft-webauthn==0.1.4
