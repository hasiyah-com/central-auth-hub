# Accuracy Gate — Hybrid RBA 4 Layers — pre-registration (2026-09-23)

ส่วนนี้ commit **ก่อน**สร้างผลใหม่ใด ๆ · ผลจะเพิ่มในส่วนท้ายโดยไม่แก้ส่วนนี้ · ตัวเลขและตรรกะทั้งหมดอยู่ใน
`ml-service/scripts/hybrid_experiment/accuracy_gate.py` และถูกล็อกด้วย `hub/backend/tests/test_accuracy_gate.py`

## 1. สถานะตั้งต้น

- ฐาน: branch `feature/hybrid-accuracy-validation` แตกจาก `44869d2` (worktree สะอาด · `git diff --check` ผ่าน)
- ผลเดิมที่คงไว้: P1 v2 ไม่ผ่าน · P1 v3 (§26) ไม่ผ่าน · A/B §27–28 ไม่พบว่าตัวจำกัดทำให้ช้าลง · **Capacity Gate
  ยังไม่ผ่าน** · P48-T2 = `inconclusive` (frozen) · holdout ของ P48-T2 (16 โปรไฟล์) และ seed 101–115 **ไม่แตะ**

## 2. ตัวชี้วัดและเกณฑ์

ไม่ใช้ accuracy รวม — เหตุการณ์ปกติมากกว่าการโจมตีหลายเท่า · ทุกเกณฑ์ตัดสินจาก **cluster CI (ผู้ใช้ → seed)**

| ตัวชี้วัด | เกณฑ์ | วิธี |
|---|---|---|
| Challenge FPR | ขอบบน CI ≤ **1%** ทุกขนาดประวัติ | `gate.config_gate` (เดิม) |
| Block FPR | ขอบบน CI ≤ **0.20%** ทุกขนาดประวัติ | เดิม |
| Severe recall@challenge | ขอบล่าง CI ≥ **90%** | `hierarchical_proportion` · ตระกูลที่มี policy floor 7 ตระกูล¹ |
| Campaign recall | ขอบล่าง CI ≥ **80%** | หน่วยนับ = แคมเปญ · จับได้ถ้ามีเหตุการณ์ใดถึง **challenge** |
| Recall@challenge ตระกูลที่อ่อน | ขอบล่าง CI ≥ เป้าในตาราง 2.1 | ต่อตระกูล |
| ไม่ถดถอย | ตระกูลที่ baseline จับได้ 100% ต้องได้ 100% | ค่าจุด |
| Precision (challenge / block) | รายงาน | — |
| Abstain rate ของ L3 | รายงานแยกตามขนาดประวัติ | — |
| Expert agreement | กำหนดก่อนเปิด Pilot (ยังไม่มีผู้ตรวจ) | — |
| ความเสถียรของ calibration | รายงานอัตรา L3 ยิงบนเหตุการณ์ปกติของ validation | — |

¹ `combined_ato, concurrent_sessions, failed_spike, new_device, new_os, new_ua_family, permission_change`

### 2.1 เป้าของตระกูลที่อ่อน (recall@challenge)

| ตระกูล | P48 / P48-T2 | เป้า (ขอบล่าง CI) |
|---|---|---|
| `new_passkey` | 24% / 18% | ≥ 60% |
| `subtle_rare_device` | 26% / 14% | ≥ 60% |
| `subtle_slow_burst` | 27% / 25% | ≥ 60% |
| `campaign` | 37% / 42% | ≥ 80% |
| `login_velocity` | 42% / 38% | ≥ 70% |
| `subtle_quiet_lateral` | 64% / 68% | ≥ 75% |

### 2.2 กฎตัดสิน

- `passed` = ทุกข้อผ่าน · `failed` = มีข้อใดไม่ผ่าน (ชนะ inconclusive) · `inconclusive` = CI คร่อมเกณฑ์ →
  **ยังไม่พร้อม deploy**
- ห้ามเปลี่ยนเกณฑ์หลังเห็นผล · ห้ามเพิ่มรอบหลังเห็นผลโดยไม่มีแผนที่ commit ล่วงหน้า
- ข้อสังเกตที่ประกาศไว้ก่อน: ประชากรมี 32 โปรไฟล์ validation → CI ต่อตระกูลจะกว้าง · ผล `inconclusive` เป็นไปได้สูง
  และจะรายงานตามนั้น

## 3. การแบ่งข้อมูลและการเทียบที่ FPR เท่ากัน

| ชุด | ข้อมูล | หน้าที่ |
|---|---|---|
| Train / Calibration (ECDF) | split ภายในผู้ใช้ของแต่ละ cell (`DS.build`) | fit โมเดล + ECDF (เดิม) |
| **Calibration ของรอบนี้** | P48-T2 validation profiles × seed **[401–405]** (เห็นแล้ว) | ตั้ง threshold ให้ FPR เท่ากัน + เลือกพารามิเตอร์ G |
| **Validation** | P48-T2 validation profiles × seed **[501–505]** (ใหม่) | ตัดสิน gate ครั้งเดียว |
| Final holdout | holdout 16 โปรไฟล์ของ P48-T2 | **ไม่เปิดในรอบนี้** |

- ทุก candidate ตั้ง threshold บน calibration ให้ **challenge FPR ≤ 0.5% และ block FPR ≤ 0.1% ทุกขนาด**
  (`pick_threshold` บน grid 0.400–0.995 ทีละ 0.005 · ต่ำกว่างบเพื่อเผื่อ tail shift ข้ามประชากรที่เคยวัดได้
  0.73% → 1.18%) · ถ้าไปไม่ถึง (FPR จาก policy floor) ต้องรายงาน `reachable=false`
- แล้วใช้ threshold เดียวกันนั้นบน validation — ไม่ปรับอีก

## 4. Candidate

| key | ความหมาย |
|---|---|
| B | baseline L1+L2 (max + corroboration) |
| E | hybrid ปัจจุบัน — L3 point + sequence ใช้ max ร่วมกับ L1/L2 |
| **G** | **conditional L3 fusion** (ระยะที่ 5) — gate ตัดสิน G เท่านั้น |

### 4.1 G — grid และกฎการเลือก (บน calibration เท่านั้น)

`ambiguous_low ∈ {0.30, 0.40, 0.50}` × `w_point ∈ {0.5, 1.0}` × `w_sequence ∈ {0.5, 1.0}` ×
`low_zone_agree ∈ {0.90, 0.95}` = 24 ชุด · แต่ละชุดตั้ง threshold ของตัวเองตามข้อ 3 แล้วเลือกด้วย `select_g`:

1. recall@challenge เฉลี่ยของ 6 ตระกูลในข้อ 2.1 สูงสุด
2. severe recall สูงสุด
3. challenge FPR ต่ำสุด
4. น้ำหนักรวมน้อยสุด

ชุดที่ไปไม่ถึง FPR เป้าถูกตัดทิ้งก่อน · เลือกแล้ว freeze ก่อนแตะ validation

### 4.2 การออกแบบของ G ที่ตรึงไว้ตอน implement (ก่อนวัดใด ๆ · commit ระยะที่ 5)

- ขอบบนของช่วงกำกวม `AMBIGUOUS_HIGH` = **0.70 คงที่** (ค่า challenge เริ่มต้นของ production) ไม่ผูกกับ threshold
  ที่กวาด → คะแนนของ G ไม่ขึ้นกับ threshold จึงกวาดหา FPR เท่ากันผ่าน `resolve_action` ตัวเดียวกับ B/E ได้
- "ยกได้สูงสุด warn" ในช่วงเสี่ยงต่ำ ส่งผ่าน `ResolverInput.action_cap` (ใหม่ · ค่าเริ่มต้น None ไม่เปลี่ยน B/E) ·
  policy floor ชนะเพดานเสมอ · L3 ห้าม block คนเดียวทุกช่วง (`SOLO_BLOCK_FORBIDDEN` เดิม)
- ช่วง high ใช้ L3 เป็นหลักฐานสนับสนุนด้วย gamma เดียวกับ fusion เดิม
- production: `L3_CONDITIONAL_PARAMS` (JSON) ว่าง = ไม่คำนวณ · ตั้งแล้วบันทึก `conditional_shadow` ใน
  `risk_breakdown` เป็นผลจำลองชุดที่สาม เฉพาะโหมด `shadow_hybrid`/`hybrid_stepup` · key ผิด/ค่าผิดช่วง = ไม่ start

## 5. ห้ามทำ

- ปรับ threshold หรือพารามิเตอร์หลังเห็น validation แล้ววัดชุดเดิมซ้ำ
- เลือก candidate ที่ผ่านแบบ post-hoc (gate ตัดสิน G ตัวเดียว)
- ใช้ accuracy รวมกลบผลของตระกูล
- เปิด holdout เมื่อ validation ไม่ผ่าน · เปิด L3 enforcement

## 5.1 Amendment 1 — แก้ก่อนวัด (2026-09-23)

พบตอนเตรียมสคริปต์วัด **ก่อนมีผล validation ใด ๆ** · ข้อมูลที่ดูมีเพียง probe บน **calibration** seed 401 size 50
(เวลา + การกระจายคะแนนของ E บนเหตุการณ์ปกติ) ไม่ได้ดู recall ของ candidate ใด

| ข้อ | เดิม | แก้เป็น | เหตุผล |
|---|---|---|---|
| threshold grid | 0.400–0.995 ทีละ 0.005 | เดิม + 0.9951–0.9999 (0.0001) + 0.99991–0.99999 (0.00001) + 0.999991–0.999999 (0.000001) | frozen config ของ B/E ใช้ gamma 1.0 และ threshold 0.995 / 0.9999 · คะแนนปกติของ E ที่ q99.5 = 0.99977 → grid เดิมไปไม่ถึง FPR 0.5% ทุก candidate โดยโครงสร้าง |
| gamma | ไม่ได้ระบุ | `per_config_gamma` ของ Round 2c (B = 1.0, E = 1.0) · G ใช้ของ E | ให้ G ต่างจาก E เฉพาะวิธีรวม L3 |
| warn threshold | ไม่ได้ระบุ | `min(warn เดิมของ config, challenge ที่ตั้งใหม่)` · G ใช้ warn ของ E | warn ไม่ใช่เกณฑ์ตัดสิน |
| Campaign recall | "หน่วยนับ = แคมเปญ" | กลุ่มการโจมตี (ผู้ใช้ × scenario × seed × size) จับได้ถ้ามีเหตุการณ์ใดถึง challenge · ต่างจากตระกูล `campaign` ในข้อ 2.1 (ระดับเหตุการณ์) | คำว่า "แคมเปญ" ใช้ซ้ำสองความหมาย |

ข้อสังเกตที่ประกาศไว้ก่อนวัด: `AMBIGUOUS_HIGH = 0.70` ของ G อยู่บนสเกลเดียวกับคะแนน L1/L2 ที่ calibrate แล้ว
(ค่ากลางของ E บนเหตุการณ์ปกติ ≈ 0.69) → ราวครึ่งหนึ่งของเหตุการณ์ปกติอยู่ช่วง high · ไม่แก้ (ออกแบบและ commit แล้ว)
และจะรายงานสัดส่วนเหตุการณ์ต่อช่วงในผล

## 6. ผล — 2026-09-23

- calibration: seeds [401–405] × 5 ขนาด × 26 variants (B, E, G 24 ชุด) · 17,266 เหตุการณ์ต่อ cell ·
  `caught_fast` ถูกตรวจเทียบกับ `resolve_action` ทุก variant (parity 1,000 ตัวอย่าง/variant) ·
  ผลอยู่ใน `ml-service/data/hybrid_experiment/accuracy_gate/calibration.json` · สำเนาที่ตรึงไว้ commit `fb4cca0`
  (`hub/backend/tests/provenance/accuracy_calibration.json`)
- validation: seeds [501–505] (ไม่เคยเห็น) เปิด**ครั้งเดียว** · ledger `validation_ledger.json` ·
  holdout ของ P48-T2 ไม่ถูกแตะ (`holdout_opened: false`)

### 6.1 คำตัดสิน: **failed**

G ไม่ผ่าน 7 ข้อ: `campaign_recall` และตระกูลที่อ่อนทั้ง 6 ตระกูล · ไม่มีข้อไหน `inconclusive`
(CI แคบพอจะสรุปได้ว่า**ต่ำกว่าเป้าจริง** ไม่ใช่ข้อมูลไม่พอ)

### 6.2 threshold ที่ FPR เท่ากัน (ตั้งบน calibration)

| candidate | challenge | block | ถึงเป้า FPR |
|---|---|---|---|
| B | 0.9984 | 0.9996 | ใช่ |
| E | 0.99998 | 0.99998 | ใช่ |
| G (params `ambiguous_low=0.30, w_point=0.5, w_sequence=0.5, low_zone_agree=0.90`) | 0.99998 | 0.999995 | ใช่ |

การเพิ่มหลักฐาน L3 ดันคะแนนของ**เหตุการณ์ปกติ**ขึ้นด้วย → threshold ที่ให้ FPR เท่ากันต้องสูงขึ้นมาก
(0.9984 → 0.99998) ซึ่งเป็นกลไกหลักที่ทำให้ E และ G เสียการจับที่ B ทำได้

### 6.3 เกณฑ์ที่ผ่าน (ทั้งสาม candidate)

| เกณฑ์ | B | E | G |
|---|---|---|---|
| Challenge FPR (งบ 1%) | ผ่าน 0.445–0.500% | ผ่าน 0.415–0.462% | ผ่าน 0.445–0.481% |
| Block FPR (งบ 0.20%) | ผ่าน 0.049–0.061% | ผ่าน 0.044–0.066% | ผ่าน 0.051–0.070% |
| Severe recall (≥ 90%) | ผ่าน **1.000** | ผ่าน 1.000 | ผ่าน 1.000 |
| ไม่ถดถอย (ตระกูลที่ B ได้ 100%) | — | ผ่าน | ผ่าน |
| Precision challenge / block | 0.910 / 0.983 | 0.880 / 0.978 | 0.897 / 0.980 |

7 ตระกูลที่มี policy floor จับได้ 100% ทุก candidate — floor ไม่ขึ้นกับคะแนน จึงไม่ถูกกระทบจาก threshold ที่สูงขึ้น

### 6.4 เกณฑ์ที่ไม่ผ่าน

| ตัวชี้วัด | เป้า | B | E | **G** |
|---|---|---|---|---|
| Campaign recall (ขอบล่าง CI) | ≥ 0.80 | 0.733 [0.699, 0.769] | 0.500 [0.474, 0.531] | **0.642 [0.610, 0.679]** |
| `new_passkey` | ≥ 0.60 | 0.083 | 0.023 | **0.061 [0.022, 0.113]** |
| `subtle_rare_device` | ≥ 0.60 | 0.061 | 0.030 | **0.054 [0.008, 0.114]** |
| `subtle_slow_burst` | ≥ 0.60 | 0.135 | 0.037 | **0.075 [0.024, 0.143]** |
| `campaign` (ระดับเหตุการณ์) | ≥ 0.80 | 0.311 | 0.197 | **0.238 [0.158, 0.326]** |
| `login_velocity` | ≥ 0.70 | 0.283 | 0.051 | **0.083 [0.040, 0.141]** |
| `subtle_quiet_lateral` | ≥ 0.75 | 0.609 | 0.170 | **0.529 [0.403, 0.652]** |

### 6.5 เทียบแบบจับคู่ (เหตุการณ์เดียวกัน · bootstrap ระดับผู้ใช้)

| คู่ | severe | ตระกูลที่อ่อน (รวม 6 ตระกูล) |
|---|---|---|
| G − B | 0.000 [0.000, 0.000] | **−0.075 [−0.095, −0.054]** |
| G − E | 0.000 [0.000, 0.000] | **+0.070 [+0.054, +0.088]** |

**อ่านได้ว่า:** conditional fusion ดีกว่า hybrid ปัจจุบันอย่างมีนัย (+7.0 จุด) แต่ยัง**แย่กว่า baseline L1+L2
อย่างมีนัย** (−7.5 จุด) · CI ทั้งสองไม่คร่อมศูนย์

### 6.6 คุณค่าของ L3 (G เทียบ B บนเหตุการณ์เดียวกัน)

| รายการ | ค่า |
|---|---|
| เหตุการณ์โจมตีที่ผลต่างกัน | 3,316 |
| **จับได้เพิ่มเพราะ G** | 246 |
| **จับไม่ได้เพราะ G (B จับได้)** | 3,070 |
| สุทธิ | **−2,824** |
| ผลรวม challenge FPR ทุกขนาด (G − B) | −0.05 จุด (G ต่ำกว่าเล็กน้อย) |

### 6.7 ช่วงของ L1/L2 ที่เหตุการณ์ปกติตกอยู่ (G)

| ขนาดประวัติ | low | ambiguous | high | L3 ยิงบน normal | abstain |
|---|---|---|---|---|---|
| 50 | 67.6% | 8.3% | 24.2% | 0.006% | 0% |
| 100 | 69.3% | 3.3% | 27.4% | 0.013% | 0% |
| 500–5000 | 71.8–72.7% | ~0% | 27.3–28.2% | 0.006–0.010% | 0% |

ช่วง "กำกวม" ที่ออกแบบให้ L3 ช่วยแยก กินเพียง 0–8% ของเหตุการณ์ปกติ และแทบหายไปเมื่อประวัติยาวขึ้น ·
`AMBIGUOUS_HIGH = 0.70` ที่ตรึงไว้ก่อนวัด (§4.2) จึงไม่ตรงกับการกระจายจริงของคะแนน L1/L2 ในประชากรนี้

### 6.8 สรุปและสิ่งที่ตามมา

1. **Accuracy Gate ไม่ผ่าน** — G ไม่ถึงเป้าของตระกูลที่อ่อนและ campaign recall · ไม่เปิด holdout ·
   ไม่เปิด Shadow Pilot · ไม่เปิด L3 enforcement
2. **ที่ FPR เท่ากัน L3 ในรูปแบบปัจจุบันทำให้แย่ลง** — ทั้ง E และ G จับได้น้อยกว่า baseline L1+L2 ·
   กลไกคือหลักฐาน L3 ดันคะแนนของเหตุการณ์ปกติขึ้นด้วย ทำให้ threshold ที่คุม FPR ต้องสูงขึ้น
   และการจับที่มาจาก "คะแนน" (ไม่ใช่ policy floor) หายไป
3. **conditional fusion แก้ได้บางส่วน** (+7.0 จุดเทียบ E) เพราะจำกัดอิทธิพลของ L3 ในช่วงเสี่ยงต่ำ
   แต่ไม่พอจะชนะ baseline
4. ผลนี้สอดคล้องกับข้อจำกัดที่บันทึกไว้ตั้งแต่ freeze 29 ส.ค.: L3 ยังไม่มีหลักฐานว่าเพิ่มคุณค่าบนข้อมูลจริง
5. ทางเลือกถัดไป (ต้องเลือกและ pre-register ก่อนวัดใหม่ · ห้ามวัดชุดเดิมซ้ำ):
   (ก) ปรับ calibration ของ L3 ให้หลักฐานบนเหตุการณ์ปกติต่ำลง แทนการปรับวิธี fusion
   (ข) นิยามช่วงกำกวมจากควอนไทล์ของคะแนน L1/L2 จริงแทนค่าคงที่ 0.70
   (ค) ใช้ L3 เป็นช่องเฝ้าระวังอย่างเดียว (ไม่เข้าแกนการตัดสิน) ซึ่งเป็นสถานะปัจจุบันของ production
   (ง) ยอมรับว่าที่ FPR เท่ากัน L3 ยังไม่เพิ่มคุณค่า และรายงานตามนั้นในเล่ม

### 6.9 ข้อจำกัดของรอบนี้

- ข้อมูลสังเคราะห์จากประชากร P48-T2 32 โปรไฟล์ · label มาจากการสร้าง ไม่ใช่การโจมตีจริง
- recall ของตระกูลที่อ่อนวัดบนผู้ใช้ 32 คน → CI กว้างระดับ ±5–13 จุด
- Expert agreement ยังไม่มี (ไม่มีผู้ตรวจในรอบนี้) · ความเสถียรของ calibration รายงานเป็นอัตรา L3 ยิงบน
  เหตุการณ์ปกติเท่านั้น
- SHAP ไม่ได้ตรวจในรอบนี้ (harness ไม่ได้เก็บ) — ข้อจำกัดเดิมของ B67 ยังอยู่

## 7. L3 ablation — เขียนก่อนวัด (2026-09-23)

ส่วนนี้ commit **ก่อน**วัด · ผลจะเพิ่มใน §8 โดยไม่แก้ส่วนนี้ · **ผล §6 (Accuracy Gate = failed) คงไว้ตามเดิม**
ไม่ว่าผล ablation จะออกมาอย่างไร · ไม่เปิด Final Holdout

### 7.1 คำถาม

§6 บอกว่า "L3 ที่ FPR เท่ากันทำให้แย่ลง" แต่ไม่ได้บอกว่าเพราะ **มุมมองไหน** และเพราะ **ไม่มีสัญญาณ**
หรือเพราะ **สเกลของหลักฐานผิด** · ablation นี้ตอบสองข้อนั้นเพื่อเลือกระหว่างปรับ calibration กับคง L3
เป็นช่องเฝ้าระวังอย่างเดียว

### 7.2 แขนที่วัด (ทุกแขนเรียก config production ตัวจริง)

| แขน | คือ |
|---|---|
| B | L1 + L2 (baseline) |
| C | B + L3 point อย่างเดียว |
| D | B + L3 sequence อย่างเดียว |
| E | B + ทั้งสองมุมมอง (hybrid ปัจจุบัน) |
| G | conditional L3 fusion · params ที่ freeze แล้ว (`fb4cca0`) ไม่เลือกใหม่ |

### 7.3 ขั้นตอน

1. **ตั้ง threshold ให้ FPR เท่ากัน** บน calibration seeds [401–405] (ชุดที่เห็นแล้ว) ด้วยกฎเดิมของ §3
   (challenge ≤ 0.5%, block ≤ 0.1% ทุกขนาด) — ทุกแขนรวมทั้ง C และ D
2. **วัดบน seed ใหม่ [601–605]** (ไม่เคยใช้ · ไม่ทับ 401–405, 501–505, 101–115) × 5 ขนาดเดิม
3. ไม่ปรับ threshold ไม่เลือก candidate ใหม่ ไม่เพิ่มรอบหลังเห็นผล

### 7.4 ตัวชี้วัด

- **สัญญาณของแต่ละมุมมอง:** AUC ของหลักฐานมุมมองนั้น**ล้วน** (normal เทียบ attack) + CI จาก bootstrap
  ระดับผู้ใช้ — วัดจากแขน E ซึ่งมีทั้งสองมุมมอง
- **ราคาที่จ่าย:** threshold ที่แต่ละแขนต้องใช้เพื่อให้ FPR เท่ากัน + ควอนไทล์คะแนนของเหตุการณ์ปกติ
  (q50/q90/q99/q99.5/q99.9) — แขนที่ดัน normal ขึ้นมากจะต้องใช้ threshold สูงกว่า
- **ผลลัพธ์:** recall ของตระกูลที่อ่อน (เฉลี่ย 6 ตระกูล), severe recall, campaign recall,
  และ paired delta เทียบ B บนเหตุการณ์เดียวกัน (bootstrap ระดับผู้ใช้)

### 7.5 กฎแปลผล (ประกาศก่อนวัด · `ablation_conclusion`)

"มุมมองมีสัญญาณพอใช้" = AUC ≥ **0.70** และขอบล่าง CI ≥ **0.60**

| ข้อสรุป | เงื่อนไข | สิ่งที่ตามมา |
|---|---|---|
| `l3_helps` | มีแขนใดชนะ B อย่างมีนัย (ขอบล่าง paired delta > 0) | เสนอรอบวัดใหม่ของแขนนั้น |
| `recalibrate` | ไม่มีแขนชนะ · มีมุมมองที่มีสัญญาณ · มีแขนที่แพ้อย่างมีนัย | ตัวเลือก **ก** — ปัญหาอยู่ที่สเกลของหลักฐาน ไม่ใช่ตัวสัญญาณ |
| `monitoring_only` | ไม่มีมุมมองใดมีสัญญาณ และมีแขนที่แพ้อย่างมีนัย | ตัวเลือก **ค** — คง L3 เป็นช่องเฝ้าระวัง |
| `inconclusive` | นอกนั้น | ไม่เปลี่ยนอะไร · รายงานตามจริง |

### 7.6 สิ่งที่จะไม่ทำ

เปิด Final Holdout · เปลี่ยนเกณฑ์หรือ params หลังเห็นผล · ใช้ผล ablation แทนผล Accuracy Gate ·
เปิด Shadow Pilot หรือ L3 enforcement

## 8. ผล ablation

(เพิ่มหลังวัด)
