# 2026-05-22

## Session Goal
ตัดสินใจ architecture เรื่อง worktree DB (เลือก Strategy A) + merge งาน parallel จาก ml-dev + dorm-dev + library — แล้ว clean up infrastructure

## ทำอะไรบ้าง

**🎨 Subsystem A (Dorm) — major refactor**
- Redesign เป็น React SPA + Bauhaus theme (commit `3425255`)
- Merge `feature/dorm-dev` กลับ main (`4ed34b0`)

**📚 Subsystem B (Library) — UI + Auth**
- Vintage UI + sidebar layout + Hub registration (`319d82f`)
- Expand JWT scope เป็น 4 fields + redesign `/me` page (`40fa3a8`)

**🧠 ML Service — eval methodology**
- 80/20 train/test split → retrain iforest_v1 (`f07fc06`)
- Merge `feature/ml-dev` 2 รอบ (`83b1a75`, `7e56dc1`) — P0 improvements + GeoIP backfill
- เพิ่ม GeoLite2 db (`hub/backend/data/GeoLite2-Country.mmdb` ~9MB, gitignored)

**🏗️ Infrastructure decision**
- เลือก **Strategy A** — Single-stack Docker (main เท่านั้น) — เพราะ worktree DB แยก → test ไม่ consistent (Gmail admin ไม่อยู่ใน worktree DB, OAuth callback ชี้ผิด port)
- Stop ทุก worktree stack (`cah-hub`, `central-auth-starter-ml`)
- เจอ B33 อีกรอบ — `docker compose` attach container เดิมที่อ่าน `.env` ผิด folder → fix ด้วย `--force-recreate`
- Update `scripts/worktree/README.md` บันทึก Policy
- Fix `.gitignore` หาย entries จาก merges — restore + เพิ่ม `*.mmdb`

## System Test — Before
| Service | Status |
|---------|--------|
| Hub /health | ✅ 200 |
| Hub /health/db | ✅ 200 |
| Hub JWKS | ✅ 200 |
| Subsystem A | ✅ 302 |
| Subsystem B | ✅ 302 |
| ML /health | ✅ 200 |

## System Test — After
| Service | Status |
|---------|--------|
| Hub /health | ✅ 200 |
| Hub /health/db | ✅ 200 |
| Hub JWKS | ✅ 200 |
| Subsystem A | ✅ 302 |
| Subsystem B | ✅ 302 |
| ML /health | ✅ 200 |

## Commits Today
```
98c8d69 fix(gitignore): restore lost entries + add GeoLite2 db ignore
764ec3e docs: worktree README — adopt Strategy A (single-stack Docker policy)
7e56dc1 merge: feature/ml-dev — P0 ML eval split + GeoIP + backfill
f07fc06 chore(ml): retrain iforest_v1 with 80/20 split
40fa3a8 library: expand JWT scope to 4 fields + redesign /me page
4ed34b0 merge: feature/dorm-dev — Subsystem A React SPA + Bauhaus theme
319d82f library: vintage UI redesign + sidebar layout + Hub registration
3425255 ui: redesign Subsystem A (dorm) as React SPA + Bauhaus theme
83b1a75 merge: feature/ml-dev — P0 ML improvements
```

## Bug ใหม่ที่เจอ

**B35. `.gitignore` lost entries during merges**
- อาการ: หลัง merge `feature/ml-dev` และ `feature/dorm-dev` กลับ main → `.gitignore` กลับไปเป็นเวอร์ชั่นเก่า → `.claude/settings.local.json`, `tmp_*.py`, `docker-compose.override.yml` โผล่เป็น untracked อีกครั้ง
- สาเหตุ: branch feature/* เริ่มต้นก่อน commit `2493845` ที่เพิ่ม entries → merge ทับ
- **กฎ:** ทุกครั้งหลัง merge → `git status` ตรวจ untracked ที่ควรเป็น ignored → fix ทันที
- **Verify:** `git check-ignore -v <file>` ต้องคืน rule ที่ match

## Next Session

**Pre-work ก่อน Week 8:**
- [ ] แก้ pytest path ใน `dev-routine/SKILL.md` (B34 จากเมื่อวาน) — เปลี่ยน `pytest hub/backend/` เป็น `pytest .` (container WORKDIR=/app)
- [ ] Install pre-commit: `pip install pre-commit && pre-commit install`
- [ ] รีวิว Library UI ที่ stash ไว้ → `git stash pop` แล้วตัดสินใจ commit/discard (overlap กับ vintage UI ที่ commit ไปแล้ว?)

**Week 8 — Admin Dashboard (Next.js):**
- `cd hub && npx create-next-app@14 frontend --typescript --tailwind --app --eslint`
- ก่อนเริ่ม: พิมพ์ `/frontend-admin` เพื่อโหลด context
- Pages: `/dashboard`, `/users`, `/subsystems`, `/audit`, `/subsystems/pending`
- TDD: เขียน test ก่อน (Jest + React Testing Library)

**Pending decisions:**
- ML eval split policy → document ใน `ml-service/SKILL.md` (80/20 + metric thresholds)
- React SPA pattern ใน Dorm → ถ้าจะใช้กับ Library + Admin Dashboard ด้วย ควร extract เป็น shared template
