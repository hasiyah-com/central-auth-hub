# 2026-06-09

## Session Goal
เพิ่ม LINE Login เป็น alternate IdP (พิสูจน์ flexibility ของ OAuth platform) + อัปเดต B-list (B33-B41) + อัปเดต CLAUDE.md Roadmap

## ทำอะไรบ้าง

**🟢 LINE Login (alternate OAuth IdP)**
- ตัดสินใจหลัง Microsoft Entra ID block personal account ที่ไม่มี Azure subscription (ต้องมีบัตรเครดิต)
- LINE = ฟรี, ไทยใช้เยอะ, OIDC compliant → pattern เดียวกับ Google
- Backend: `oauth.register(name="line")` + `/line/login` + `/line/callback` (mirror Google flow)
- DB: เพิ่ม `users.line_sub` column (manual SQL migration — ยังไม่มี alembic)
- Frontend: ปุ่มเขียว `#06C755` + LINE logo SVG ใน login page
- เอกสาร: `docs/guides/add-line-login.md` (604 บรรทัด, 7 phases)
- Commit: `304b749 feat(auth): add LINE Login as alternate OAuth IdP`

**📝 B-list updates (B33-B41 — 9 bugs ใหม่)**
- B33: docker compose attach container → อ่าน .env ผิด folder
- B34: pytest path ใน skill ผิด (container WORKDIR=/app)
- B35: .gitignore หาย entries หลัง merge
- B36: `docker compose restart` ไม่ re-read .env → ต้อง `--force-recreate`
- B37: Volume namespace เปลี่ยน (Migration B) → ข้อมูลหาย ถ้าไม่ pin name
- B38: Frontend files in worktree ไม่ commit → หายตอนลบ folder
- B39: SHAP sign convention — flip needed for "positive = anomaly" UI
- B40: LINE Channel ID vs Bot User ID confused (เพิ่งเจอ)
- B41: docker-compose.yml hub-frontend block ไม่ committed → silent risk

**📊 CLAUDE.md Roadmap update**
- Week 8: ✅ completed
- Week 8.5 (ใหม่): Migration B + SHAP + LINE + RBA 4-Layer + secret rotation = ✅
- Week 9-10 (next): MFA wire-up + Session Downgrade + Token Revocation
- เพิ่ม "สถานะเพิ่มเติม" section อธิบายสิ่งที่ทำเกิน roadmap เดิม
- เพิ่ม "Strategy A" note

**🔧 LINE Login debug session**
- เริ่มแรก: LINE_CLIENT_ID=`U40f2d4...` (เป็น Bot User ID ผิด) → 400 Bad Request
- แก้ → ใช้ Channel ID จริง `2010297925` (numeric 10 digits)
- ยังไม่ work → ค้นพบ B36: `docker compose restart` ไม่ re-read .env
- ใช้ `docker compose up -d --force-recreate hub-backend` → ค่าใหม่ใช้ได้

## System Test
| Service | Status |
|---------|--------|
| Hub /health | ✅ 200 |
| Hub /health/db | ✅ 200 |
| Hub JWKS | ✅ 200 |
| Subsystem A | ✅ 302 |
| Subsystem B | ✅ 302 |
| Frontend Admin | ✅ 307 |
| ML /health | ✅ 200 |

(7/7 PASS — รัน before commit `304b749`)

## Commits Today
```
304b749 feat(auth): add LINE Login as alternate OAuth IdP
```
+ pending: B-list + roadmap update (กำลังจะ commit)

## Bug ใหม่ที่เจอ
ดู B40 (LINE Channel ID confusion) — บันทึกใน CLAUDE.md แล้ว

## Next Session

**Pre-flight ก่อนเริ่ม:**
- ลอง LINE login จริง (ตอนนี้ container ได้ Channel ID ถูก แล้ว)
- ดู `/admin/audit` มี `provider: line` ในไม่
- ตรวจ SHAP/RBA scoring ยังทำงานบน LINE session

**Week 9 Phase 1 (ตามแผน plan file):**
- Session Downgrade — `restricted` claim ใน JWT + `require_write_access()` dependency
- Wire MFA challenge branch ใน oauth.py (mfa.py scaffold พร้อมแล้ว)
- Admin `/sessions` page เพื่อดู restricted sessions
- หรือ — Token Revocation (jti + Redis blacklist) ก่อน MFA?

**Cleanup:**
- มี untracked files ที่ค้าง (admin.py, docker-compose.yml, audit/page.tsx etc.) — ตัดสินใจ commit/discard ในรอบต่อไป
