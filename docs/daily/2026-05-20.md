# 2026-05-20

## Session Goal
สร้าง Development Routine ครบวงจร — scripts/routine/ + skill + daily log template

## ทำอะไรบ้าง
- สร้าง `scripts/routine/morning.sh` — docker health + git status + recent errors + roadmap reminder
- สร้าง `scripts/routine/test_workflow.sh` — smoke test 6 services (Hub/health, /health/db, JWKS, Dorm, Library, ML)
- สร้าง `scripts/routine/eod.sh` — pre-commit + diff summary + commit message guide
- สร้าง `.claude/skills/dev-routine/SKILL.md` — skill ที่ Claude Code follow เมื่อพูดว่า "เริ่มงาน"/"เลิกงาน"
- สร้าง `docs/daily/` directory พร้อม template วันนี้เป็น first entry
- อัปเดต `CLAUDE.md` — เพิ่ม "Development Routine" section ใน Running The Project

## System Test — Before
(services not running today — dev session เป็น config/scripting ไม่ต้อง docker)

| Service | Status |
|---------|--------|
| Hub /health | ⏭️ skipped (services down) |
| Hub /health/db | ⏭️ skipped |
| Hub JWKS | ⏭️ skipped |
| Subsystem A | ⏭️ skipped |
| Subsystem B | ⏭️ skipped |
| ML /health | ⏭️ skipped |

## System Test — After
(ไม่ได้แก้ backend code — ไม่ต้อง retest services)

## Commits Today
(รอ commit session นี้ — hook/skill/worktree/routine รวม commit เดียว)

## Bug ใหม่ที่เจอ
- `curl ... || echo "000"` ทำให้ HTTP code เป็น `000000` แทน `000` (doubled output) → แก้เป็น `; true` แทน

## Next Session
- Week 8: เริ่ม Next.js Admin Dashboard frontend
- ตั้ง Next.js project ใน `hub/frontend/` และ setup route structure
