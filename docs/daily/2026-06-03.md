# 2026-06-03

## Session Goal
ตัดสินใจ architecture เรื่อง subsystem separation + เริ่ม Migration B (split docker-compose) + ปิดรูทีนค้าง

## ทำอะไรบ้าง

**🏗️ Architecture decision — Subsystem separation**
- เปรียบเทียบ 4 ตัวเลือก: Compose profiles / Split files / Separate repos / Hybrid
- ตอบ 3 คำถามสำคัญ: Hub+ML เป็น stack เดียว (ใช่), JWKS public (ใช่), subsystem owner deploy ผ่าน HTTPS
- เปรียบ B vs C → **เลือก B** (split compose files + external network)
- Timing: **ทำเลย** เพราะ orthogonal กับ application work + Week 9-13 จะได้ใช้ split stack ตลอด

**📋 Planning**
- บันทึกแผน Week 9-13 ที่ `C:\Users\hasiy\.claude\plans\claud-md-skill-md-hidden-falcon.md`
- Phase 1 (Wk9): Session Downgrade — refer `docs/p2-session-downgrade-plan.md`
- Phase 2 (Wk10): Token Revocation — jti + Redis blacklist + admin /sessions
- Phase 3 (Wk11-12): Hardening — rate limit + CSRF + CSP + threat-model.md
- Phase 4 (Wk13): Test infrastructure — pytest + Jest + GitHub Actions

**📊 พบงาน Week 9-12 commit แล้วบางส่วน (จาก session อื่น):**
```
bf7050c feat(week9-12): approval workflows, notifications, threat-intel + ML hardening
cf9da9d feat: hybrid RBA 4-layer risk scoring + security hardening + MFA scaffold
b802df4 feat(week8): ML dashboard 4 pages + API alerts + IP blacklist
0bb7718 library: add logout link to sidebar
```
→ ต้อง review ภายหลังว่าครอบคลุมแผน Phase 1-3 แค่ไหน

**🐳 Migration B — กำลังจะเริ่ม**
- Split `docker-compose.yml` เป็น 4 files: hub/dorm/library (ml อยู่กับ hub)
- ใช้ external network `cah-net` ให้ subsystems resolve `hub-backend` ได้
- Update routine scripts + docs

## System Test — Before
| Service | Status |
|---------|--------|
| Hub /health | ✅ 200 |
| Hub /health/db | ✅ 200 |
| Hub JWKS | ✅ 200 |
| Subsystem A | ✅ 302 |
| Subsystem B | ✅ 302 |
| Frontend Admin | ✅ 307 |
| ML /health | ✅ 200 |

## System Test — After
(จะกรอกหลัง Migration B เสร็จ)

## Commits Today
ยังไม่มี commit ของ session นี้ (focus on planning + cleanup) — กำลังจะ start Migration B

## Bug ใหม่ที่เจอ
ไม่มี — มีเรื่อง observation:
- หลายไฟล์ใหม่ (`email_service.py`, `ml/` pages) **เคยอยู่ใน working tree แล้วถูก commit** ผ่าน session อื่น ระหว่างที่กำลังคุยกัน
- ดี — แต่ก็เป็น signal ว่าอาจมี Claude window อื่นเปิดอยู่ ระวัง concurrent edits

## Next Session

**ต่อจากวันนี้ (Migration B):**
1. Split `docker-compose.yml` → `docker-compose.yml` (Hub+ML), `docker-compose.dorm.yml`, `docker-compose.library.yml`
2. สร้าง external network `cah-net` + bootstrap script
3. Update `scripts/routine/*.sh` รองรับ multi-stack
4. Update `CLAUDE.md` + `README.md`
5. Test 7/7 PASS ใน multi-stack mode

**หลัง Migration B:**
- Review commits `bf7050c` + `cf9da9d` + `b802df4` ดูว่าครอบคลุม Phase 1-3 แค่ไหน
- Phase 4 (Test infrastructure) ยังไม่ได้ทำแน่ๆ — เริ่มได้
