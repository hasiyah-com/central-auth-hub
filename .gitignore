# ============ Design assets ============
design-bundle/
design-bundle.bin

# ============ Secrets ============
.env
.env.local
.env.*.local
*.pem
*.key
keys/

# ============ Python ============
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# ============ Node ============
node_modules/
.next/
dist/
build/
*.log

# ============ IDE ============
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# ============ Database ============
*.db
*.sqlite

# ============ DB backups (contain personal data — back up off-site) ============
backups/
*.sql
*.sql.gz

# ============ Docker ============
postgres_data/

# ============ pre-commit / hooks ============
.secrets.baseline.local

# ============ Worktree (per-slot override, auto-generated) ============
docker-compose.override.yml

# ============ Claude Code internal ============
.claude/settings.local.json
.claude/worktrees/

# ============ Temp / scratch files ============
hub/backend/tmp_*.py

# ============ GeoIP database (download separately, large binary) ============
hub/backend/data/*.mmdb
hub/frontend/tsconfig.tsbuildinfo
